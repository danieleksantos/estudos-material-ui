# 🌐 Material UI (MUI) - React + TypeScript + Vite

Este projeto de estudos é uma página web React desenvolvida em TypeScript que explora o uso da biblioteca **Material UI (MUI)** para criação de interfaces modernas, acessíveis e totalmente personalizáveis. O foco foi construir uma **página de login simples**, implementando também **troca de tema** entre claro e escuro com **cores personalizadas**.

---

## ✨ Funcionalidades

O projeto demonstra conceitos essenciais do MUI:

1. **Página de Login:**  
   Estruturada com componentes do MUI como `TextField`, `Button`, `FormControl` e `Paper`, garantindo acessibilidade, responsividade e semântica.

2. **Tema Customizado:**  
   Criação de um tema próprio usando `createTheme`, definindo paletas personalizadas, tipografia e espaçamentos.

3. **Troca de Tema (Light/Dark Mode):**  
   Implementação de alternância dinâmica usando `ThemeProvider` e `useState`, garantindo mudança instantânea do tema em toda a aplicação.

4. **Design Responsivo com Grid & Box:**  
   Layout construído com o sistema de grid do MUI e uso extensivo de `sx` para estilização rápida e acessível.

---

## 🛠️ Tecnologias Utilizadas

- **React** & **TypeScript**
- **Material UI (MUI):** Biblioteca de componentes moderna, acessível e altamente personalizável.
- **Vite:** Ferramenta rápida de build e desenvolvimento.
- **React Hooks:** `useState` para controle de temas e estados da interface.

---

